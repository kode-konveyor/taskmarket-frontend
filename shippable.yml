build_image: node:12
language: javascript

integrations:
  key:
    - integrationName: kodekonveyor
      type: sshKey
  generic:
    - integrationName: kkpipeline

build:
  ci:
    - npm install
    - npm run lint
    - CI=true npm run test
    - npm run mutate